import styles from './NotFoundPage.module.scss';

const NotFoundPage = () => {
  return (
    <div className={styles.wrapper}>
      <span className={styles.title}>Not Found page :(</span>
    </div>
  );
};

export default NotFoundPage;
